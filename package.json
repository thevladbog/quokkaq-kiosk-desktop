{
  "name": "quokkaq-kiosk-desktop",
  "private": true,
  "version": "0.1.1",
  "type": "module",
  "scripts": {
    "tauri": "tauri",
    "build:agent": "node scripts/build-agent.mjs",
    "predev": "node scripts/build-agent.mjs",
    "dev": "tauri dev",
    "build": "npm run build:agent && tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.10.1"
  }
}
